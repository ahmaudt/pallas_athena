<!DOCTYPE html>
<html>
  <head>
    <title>PallasAthena</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
        .flash__alert   { color: red; }
        .flash__notice  { color: blue; }
        .flash__success { color: green; }
    </style>
  </head>

  <body>
    <% if flash.any? %>
      <% flash.each do |key, value| -%>
        <section class="flash flash__<%= key %>"><%= value.html_safe %></section>
      <% end %>
    <% end %>
    <%= yield %>

    <% if advisor_signed_in? %>
    <!-- below requires method for route to work -->
    <%= link_to "Sign Out", destroy_advisor_session_path, method: :delete %>
    <% end %>
  </body>
</html>
