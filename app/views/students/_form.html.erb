<%= form_for(@student) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ugamyid, 'UGA MyID' %>
    <%= form.text_field :ugamyid %>
  </div>

  <div class="field">
    <%= form.label :preferred_name %>
    <%= form.text_field :preferred_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.collection_select :major_id, Major.all, :id, :major_code %>
  </div>

  <div class="field">
    <%= form.collection_select :advisor_id, Advisor.all, :id, :first_name %><br>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
